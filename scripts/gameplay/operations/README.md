# Operations

Operations mutate the runtime data (e.g., move ball, planned balls etc). They are part of FSM [state transitions](../state-transitions/README.md), however operations don't affect the FSM _directly_.
